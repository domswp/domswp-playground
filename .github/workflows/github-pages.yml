name: Update Pages (docs/)

on:
  push:
    branches: [main]
    paths:
      - "threejs-orbit/**"
      - "docs/index.html"
      - ".github/workflows/github-pages.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: threejs-orbit/package-lock.json

      - name: Build Orbit Viewer for Pages
        working-directory: threejs-orbit
        env:
          GITHUB_PAGES: "true"
        run: |
          npm ci
          npm run build

      - name: Copy into docs/
        run: |
          rm -rf docs/threejs-orbit/*
          mkdir -p docs/threejs-orbit
          cp -r threejs-orbit/dist/* docs/threejs-orbit/
          touch docs/.nojekyll

      - name: Commit docs if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/threejs-orbit docs/.nojekyll
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "chore(pages): refresh docs/threejs-orbit build"
            git push
          fi
