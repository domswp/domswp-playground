/** Radius Bumi nyata (km) — untuk skala ketinggian orbit. */
export const EARTH_RADIUS_KM = 6371;

/** Radius mesh Bumi di scene Three.js. */
export const EARTH_MESH_RADIUS = 1;

/** NORAD catalog id — International Space Station. */
export const ISS_NORAD_ID = 25544;

/** Periode orbit ISS ~ menit (perkiraan untuk sampling garis orbit). */
export const ISS_ORBIT_PERIOD_MIN = 92.68;

export const TLE_REFRESH_MS = 6 * 60 * 60 * 1000;
export const API_SYNC_MS = 45 * 1000;

export const EARTH_TEXTURE_URL =
  "https://cdn.jsdelivr.net/npm/three-globe@2.31.1/example/img/earth-blue-marble.jpg";
